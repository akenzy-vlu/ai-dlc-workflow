# antd and its rc-component internals are resolved at the root so a single copy of the
# design system is shared; hoisting them avoids two React contexts in one tree.
shamefully-hoist=false
strict-peer-dependencies=false
