---
id: T-03-03
uow: UOW-03
title: Bump RULESET 4 to 5 and regress the three plans already closed in this repo
layer: test
type: chore
estimate: 2h
status: done
depends_on: [T-03-02]
blocks: [T-03-04]
verifies: [AC-13]
touches:
  - skills/ai-dlc-core/scripts/uow_graph.py
  - docs/PILOT-RUNBOOK.md
assumptions: [A-09]
---

# T-03-03 — Bump RULESET 4 to 5 and regress the three plans already closed in this repo

## Context

`CLAUDE.md` calls `RULESET` "the one number that must not be changed casually — it's the
seam that lets this tooling evolve without silently invalidating plans in every repo that
uses it." This feature changes what G4 accepts, so the bump is required; the regression is
what makes it safe. Three real plans closed under ruleset 4 live in this repo and are the
only ruleset-4 evidence available without touching erp2, erp3 or jack-erp.

## Implementation notes

- `RULESET = 5`, and `__version__` bumped alongside it.
- Regress all three: `2026082801-agent-chat-console`, `2026082901-ticket-detail-drawer`,
  `2026082902-skill-file-viewer`. For each, `check G4` and `check G5` must produce the same
  verdict as before the bump — capture both outputs before changing the constant so the
  comparison is real rather than remembered.
- Warnings may differ (the migration hint is new); verdicts may not.
- Add a short post-install section to `docs/PILOT-RUNBOOK.md`: after copying core into
  `~/.claude/skills/`, run `check G4` on one closed plan per target repo and confirm the
  verdict is unchanged. That is the check erp2/erp3/jack-erp need and this repo cannot run
  for them.

## Done when

- [x] `uowg --version` reports ruleset 5
- [x] Before/after verdicts captured for all three closed plans, and identical
- [x] `docs/PILOT-RUNBOOK.md` documents the per-repo post-install check
- [x] `python3 -m py_compile` clean on both scripts
