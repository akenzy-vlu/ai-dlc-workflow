---
id: T-04-05
uow: UOW-04
title: Document the flow loop and block/unblock in SKILL.md and methodology.md
layer: interface
type: chore
estimate: 2h
status: done
depends_on: [T-04-03, T-02-05]
blocks: []
verifies: [AC-15]
touches:
  - skills/ai-dlc-core/SKILL.md
  - skills/ai-dlc-core/references/methodology.md
assumptions: []
---

# T-04-05 — Document the flow loop and block/unblock in SKILL.md and methodology.md

## Context

Last ticket in the feature, and the one that makes the rest discoverable. `SKILL.md` claims
"review lag shows up as stalled parallelism rather than as invisible debt" — until this
slice that was only true if someone happened to notice `aidlc ready` going quiet. Now there
is a command that says it, and the claim should point at it.

`depends_on` includes `T-02-05` because both write `SKILL.md`.

## Implementation notes

- Add `block`, `unblock`, `flow` and `evidence` to the Commands block, one line each.
- Under Phase 4 in `methodology.md`, describe the loop as it now works: ready → start →
  block when waiting → submit, which runs the verification → accept by someone else.
- Add the closing note to Phase 5: estimate bias from the finished feature is the input to
  the next one's estimates. That is the compounding G5 already claims and could not deliver.
- Add one row to the Common failure modes table: a ticket hand-edited to `blocked`, whose
  fix is `unblock` refusing and the user running `block` properly.
- State once, plainly, that flow numbers are per ticket and per feature and are not a
  measure of a person.

## Done when

- [x] All four new commands documented in `SKILL.md`
- [x] Phase 4 and Phase 5 in `methodology.md` describe the loop as shipped
- [x] The failure-modes table gains the hand-edited-blocked row
- [x] The not-a-performance-measure note appears exactly once, where flow is introduced
- [x] No claim in either file describes behaviour this feature did not ship
