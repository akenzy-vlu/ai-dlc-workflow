---
id: T-01-03
uow: UOW-01
title: Keep the --no-review bypass working and distinctly labelled
layer: application
type: test
estimate: 1h
status: done
depends_on: [T-01-02]
blocks: [T-01-04, T-02-03]
verifies: [AC-03]
touches:
  - skills/ai-dlc-core/scripts/aidlc.py
assumptions: []
---

# T-01-03 — Keep the --no-review bypass working and distinctly labelled

## Context

`done --no-review` is the solo path, and T-01-02 must not have closed it. The bypass already
records itself as `ticket done (review bypassed)` rather than a plain acceptance; this
ticket proves both halves still hold and guards the regression.

`blocks` includes `T-02-03` because that ticket is the next edit to `aidlc.py` in the
feature. The dependency is a write-ordering constraint, not a logical one — see
`uowg --parallel`.

## Implementation notes

- Confirm the self-acceptance branch is skipped entirely when the bypass flag is set.
- Confirm the trail entry still reads as a bypass, so an audit can tell the two apart.
- If T-01-02 wired the check before the bypass test, reorder rather than special-case.

## Done when

- [x] `done --by <same actor> --no-review` still moves the ticket to done
- [x] The trail entry says the review was bypassed, not that it was accepted
- [x] `accept` without the flag is still refused for the same actor
- [x] `python3 -m py_compile` clean
