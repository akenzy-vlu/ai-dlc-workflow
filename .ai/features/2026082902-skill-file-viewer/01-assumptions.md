---
feature: skill-file-viewer
blocking_open: 0
---

# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | UI shape is a slide-over drawer opened from the Skills page row (mirrors `agent-run-drawer`), not a dedicated routed page | high | no | Rework of `skills-page.view.tsx` row wiring + new component files; no API change | confirmed | Confirmed by Akenzy, 2026-08-29, via planning interrogation |
| A-02 | Web dependencies added are `react-markdown` and `highlight.js` | high | no | Different renderer/highlighter package, same integration shape | confirmed | Confirmed by Akenzy, 2026-08-29, via planning interrogation |
| A-03 | No deadline or release train constrains this feature | high | no | None — resequencing only | confirmed | Confirmed by Akenzy, 2026-08-29, via planning interrogation |
| A-04 | Clicking the skill's name/row opens the drawer; the existing per-installation Install/Sync button keeps its own click handler (stops propagation) so both stay usable in the same row | medium | no | Row wiring reworked; no data-layer change | confirmed | Confirmed by construction and live verification, 2026-08-29 — `skills-page.view.tsx`'s `onRow` opens the drawer; `TargetRow`'s button calls `e.stopPropagation()` first. Row-click half exercised live repeatedly; button-independence half confirmed by code (a live click was deliberately avoided — it would have written to a real `~/.claude/skills` or tracked repo). |
| A-05 | The drawer always shows the skill's **source package** file tree (`skill.sourcePath`), one tree per skill id, regardless of which installation-target row was clicked — matching how `fileCount` is already reported per skill, not per installation | high | no | Would need a per-installation file source instead of per-skill; changes the id the drawer fetches by | confirmed | Confirmed by construction and live verification, 2026-08-29 — holds by construction (one `<tr>` per skill id, not per installation target) and exercised live: opened the drawer for `ai-dlc-verify` and `profile-flutter`, each showed that skill's own correct source-package tree. |
| A-06 | File content responses are capped (proposed: 1 MB) — a file over the cap is served truncated with a "file too large to preview" notice instead of the full body | medium | no | Cap value tuned later; no shape change to the endpoint contract | confirmed | Confirmed by construction, 2026-08-29 — implemented exactly as proposed (`MAX_READ_BYTES = 1_000_000` in `skill-digest.ts`); truncation behavior covered directly in `apps/api/test/skill-files.spec.ts`, including the multibyte-boundary edge case. |
| A-07 | A file is treated as text and rendered if it is valid UTF-8 under a size-sniff; anything else (binary) renders a "preview not available for this file type" placeholder instead of raw bytes | medium | no | Detection heuristic adjusted; UI already has a placeholder state to reuse | confirmed | Confirmed by construction, 2026-08-29 — null-byte sniff plus UTF-8 decode in `readPackageFile`; `SkillFileContentPanel` renders the `Empty`/"preview not available" placeholder for `encoding: 'binary'`, never touching `content.content`. Covered by `skill-files.spec.ts`; code-reviewed live (no binary fixture exists among this repo's real skill packages to trigger it over the wire). |
| A-08 | Highlighting covers markdown, python, yaml, json and shell/bash — the extensions actually present across shipped skill packages (`SKILL.md`, `.py`, `.yaml`, `.md` references) — via `highlight.js`'s language auto-detection, with any unmatched extension falling back to plain monospaced text | medium | no | Extra languages registered later; falls back gracefully today | confirmed | Confirmed by construction and live verification, 2026-08-29, with one refinement: markdown is rendered via `react-markdown`, not `highlight.js` — the assumption's own list already separated "markdown" from the `highlight.js` languages, this just makes that split explicit in the resolution. The four `highlight.js` languages use an explicit extension map (`highlight-language-map.ts`), not auto-detection, for known extensions; unmapped extensions fall back to `highlight.js`'s `highlightAuto`, confirmed live with `requirements.txt` (renders readable, not blank or erroring). |
| A-09 | All three catalog entries (`ai-dlc-core`, `ai-dlc-verify`, `profile-flutter`) are viewable regardless of install status, because `SkillInventory.list()` already resolves `sourcePath` from the catalog independent of installation state | high | no | None — this is how the existing list endpoint already behaves | confirmed | Confirmed by live verification, 2026-08-29 — opened the drawer for all three catalog entries, including `profile-flutter` (shown as "not installed" everywhere at the time); all three trees loaded correctly regardless of installation state. |
| A-10 | No additional auth/permission check gates file content beyond whatever already fronts the console today — the Skills endpoints have no auth layer of their own to extend | high | no | A permission layer would need to wrap the two new routes too | confirmed | Confirmed by construction, 2026-08-29 — the two new routes carry no auth of their own, consistent with every other route in `SkillsController`; nothing in the codebase added a permission layer for this feature to integrate with. |
