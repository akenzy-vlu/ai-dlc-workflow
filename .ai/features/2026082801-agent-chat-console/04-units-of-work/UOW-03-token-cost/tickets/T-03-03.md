---
id: T-03-03
uow: UOW-03
title: Resolve A-05 against a real transcript
layer: test
type: test
estimate: 2h
status: done
depends_on: [T-03-02, T-01-02]
blocks: []
verifies: [AC-06]
touches:
  - apps/api/test/stream-json.spec.ts
assumptions: [A-05]
---

# T-03-03 — Resolve A-05 against a real transcript

## Context
A-05 is the one assumption in this plan still pending on a fact rather than a decision: the
`result` event's `usage` keys were not present in any fixture when the plan was written.
T-01-02 records a real run; this ticket reads the `result` frame out of that same recording
and settles it.

If the keys differ from what T-03-02 assumed, fix T-03-02 and say so in the register. A
pending assumption that quietly turns out false and gets patched in silence is the failure
the register exists to prevent.

## Implementation notes
- Assert against the recorded `result` frame, not a hand-written one
- Cover the absent case explicitly: a `result` with no `usage` yields four nulls, and the
  test name should say "not reported", not "zero"
- Update A-05's row in `01-assumptions.md`: status, and a resolution note naming the run
  and the date

## Done when
- [x] The recorded `result` frame fills all four counts, or T-03-02 is corrected so it does
- [x] The absent case is covered and asserts null, not 0
- [x] A-05 is `confirmed` or `rejected` in the register with a dated resolution note
- [x] `pnpm test` is green
