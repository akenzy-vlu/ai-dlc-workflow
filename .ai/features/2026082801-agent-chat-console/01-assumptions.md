# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Pull-request integration is a separate feature, not part of this one | high | yes | One extra bounded surface (git remote, GitHub auth, webhooks) and roughly a doubling of the UoW count | confirmed | Akenzy, 2026-08-28 — confirmed out of scope when the plan was opened |
| A-02 | Turn-based resume is enough for v1; stdin stays closed and mid-run interruption is deferred | high | yes | `AgentProcessPort` and `StreamingProcessHandle` both grow a `write()`, timeouts move from wall-clock to idle-based, and the socket needs an inbound path — a rewrite of the process layer, not an addition | confirmed | Akenzy, 2026-08-28 — settled in the intent's Out of scope; recorded as ADR-01 at G2 |
| A-03 | `claude -r <session-id> -p` accepts a new prompt on stdin and continues the same conversation | high | yes | The whole feature. With no resume there is no continuation, only re-launch | confirmed | Verified 2026-08-28 against `claude --help` on this machine: `-r, --resume [value]` — "Resume a conversation by session ID"; `/Users/akenzy/.local/bin/claude` |
| A-04 | Only `claude` gets resume in v1; `codex`, `cursor-agent` and `copilot` report `resumable: false` | high | yes | A wrong resume flag does not error — per `path-agent.catalog.ts` it opens an interactive session that hangs forever behind a pipe, holding a run slot until the timeout | confirmed | Akenzy, 2026-08-28 — resume is a declared per-agent capability, never inferred. `codex` and `cursor-agent` are not even installed here; `copilot` resolves only inside a VS Code extension directory |
| A-05 | The stream-json `result` event carries a `usage` object with `input_tokens`, `output_tokens`, `cache_read_input_tokens` and `cache_creation_input_tokens` | medium | no | The token panel ships empty and falls back to the cost figure already shown. One UoW's demo weakens; nothing else moves | confirmed | Verified 2026-08-28 against a real `claude -p --output-format stream-json --verbose` run: the `result` frame carries `usage` with `input_tokens`, `output_tokens`, `cache_read_input_tokens` and `cache_creation_input_tokens`, exactly as T-03-02 assumes. T-03-03 still owns the regression test |
| A-06 | Changing the built-in `claude` entry to `-p --output-format stream-json --verbose` is safe for existing users | medium | no | Every run's transcript rendering changes at once for anyone who never overrode `agents.json`. The translator already returns `null` for non-stream-json, so the risk is cosmetic, not functional | confirmed | ADR-01/Approach step 1, 2026-08-28 — the built-in changes. The translator returns `null` for non-stream-json, so the blast radius is transcript rendering, and it ships as its own UoW so it can be reverted alone |
| A-07 | Threads and messages fit `AGENT_RUN_STORE` without a new port | medium | no | A fourth `AGENT_*` port plus both its drivers, and an extra migration step | confirmed | ADR-02 at G2, 2026-08-28 — a reply is stored as the child run's `promptPreview`, linked by `parentRunId`. No new port, no new table |
| A-08 | Chat is console state, so no `RULESET` bump and no write into `.ai/` | high | no | If chat ever had to be durable plan state, this stops being a console feature and becomes a change to `ai-dlc-core` | confirmed | CLAUDE.md and `docs/console.md` both state the rule; `AgentActivity`'s own doc comment already classifies observation as disposable console state |
| A-09 | One `sessionId` belongs to one ticket, so a thread never spans tickets | high | no | Thread identity would have to move off the ticket and onto the session | pending | Holds as long as `launch` keeps refusing a second active run per ticket |
