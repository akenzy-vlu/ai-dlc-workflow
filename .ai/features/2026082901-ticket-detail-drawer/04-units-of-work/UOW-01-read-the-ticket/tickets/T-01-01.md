---
id: T-01-01
uow: UOW-01
title: Ticket gains context and implementationNotes
layer: application
type: feature
estimate: 2h
status: done
depends_on: []
blocks: [T-01-02]
verifies: [AC-02, AC-03]
touches:
  - apps/api/src/contexts/construction/domain/model/ticket.ts
  - apps/api/src/contexts/construction/infrastructure/filesystem-construction-plan.reader.ts
  - apps/api/src/contexts/insight/application/feature-detail.query.ts   # ticket->wire mapping, see Amendment
  - apps/api/test/ticket-sections.spec.ts   # new
assumptions: [A-01, A-02]
---

# T-01-01 — Ticket gains context and implementationNotes

## Context
`sectionBody()` already does this job for a UoW's Demo script and for intent/design
sections. Verified during elaboration (A-02) against this repo's own hardest real case —
`Implementation notes` followed immediately by an unplanned `## Amendment` section before
`## Amendment during construction
`feature-detail.query.ts` was not in the drafted `touches`. `Ticket` (domain) and
`FilesystemConstructionPlanReader` were enough to get the two fields onto the aggregate,
but `FeatureDetail.tickets` is built by an explicit field-by-field map in
`feature-detail.query.ts` — not a spread — so the two new fields never reached the HTTP
response without being listed there too. Added, and `touches` amended to match.

## Done when` — and it stops exactly where it should. This ticket wires the same function
onto a ticket file's body.

## Implementation notes
- `TicketProps` gains `context: string | null` and `implementationNotes: string | null`;
  `Ticket` gains the matching getters
- In `FilesystemConstructionPlanReader`, fill both from `ticketText` using `sectionBody`,
  the same call already used for `uow.demoScript` two lines above where tickets are parsed
- No change to `parseFrontmatter`, `checklistState`, or any other field — this is two more
  lines pulled from a body the reader already has open

## Amendment during construction
`feature-detail.query.ts` was not in the drafted `touches`. `Ticket` (domain) and
`FilesystemConstructionPlanReader` were enough to get the two fields onto the aggregate,
but `FeatureDetail.tickets` is built by an explicit field-by-field map in
`feature-detail.query.ts` — not a spread — so the two new fields never reached the HTTP
response without being listed there too. Added, and `touches` amended to match.

## Done when
- [x] A ticket with both headings returns both fields populated, trimmed, unmodified
      otherwise
- [x] A ticket with neither heading returns both fields null, not an empty string and not
      a thrown error
- [x] A ticket whose `Implementation notes` is followed by an unrelated `##` section (not
      `Done when`) still stops at that section, not past it
- [x] `pnpm test` is green
