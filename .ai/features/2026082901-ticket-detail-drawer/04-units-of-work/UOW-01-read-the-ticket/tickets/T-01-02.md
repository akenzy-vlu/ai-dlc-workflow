---
id: T-01-02
uow: UOW-01
title: Mirror context/implementationNotes on the web Ticket
layer: presentation
type: feature
estimate: 1h
status: done
depends_on: [T-01-01]
blocks: [T-01-03]
verifies: [AC-02, AC-03]
touches:
  - apps/web/src/domain/entities/feature.entity.ts
  - apps/web/src/data/models/feature.model.ts
assumptions: []
---

# T-01-02 — Mirror context/implementationNotes on the web Ticket

## Context
`Ticket` on the web side is a straight structural mirror of the API's — the mapper for
`FeatureDetail` already spreads ticket fields through without a per-field transform (verify
this while here; if it does not, extend the mapper the same way).

## Implementation notes
- Add `context: string | null` and `implementationNotes: string | null` to both the model
  and the entity, in the same position as the API contract states
- Check `feature.mapper.ts`'s ticket mapping: if it is a spread, nothing else changes; if it
  lists fields explicitly, add these two there too

## Done when
- [x] Both fields reach the web `Ticket` type unchanged from the API
- [x] `pnpm typecheck` is green
