# Assumption register

| ID | Assumption | Confidence | Blocking | Blast radius if wrong | Status | Resolution |
|----|-----------|-----------|----------|----------------------|--------|-----------|
| A-01 | Every ticket template in active use headers its prose sections `## Context` and `## Implementation notes`, matching `templates.md` | high | no | A ticket using different heading text renders an empty drawer body for those two fields — degrades to less useful, not broken | confirmed | `templates.md`'s Ticket shape and every ticket written in this repo's own `agent-chat-console` feature use exactly these two headings |
| A-02 | `sectionBody()` already handles a ticket file's markdown the same way it handles a UoW's — no ticket-specific parsing quirk (checklists, frontmatter list syntax) breaks it | high | yes | The drawer's two prose fields render garbled or truncated for every ticket | confirmed | Verified 2026-08-29 against a real ticket with the hardest case in this repo: `T-02-06.md`, whose `Implementation notes` is followed by an `## Amendment during construction` section (bullets, inline code) before `## Done when`. Both `Context` and `Implementation notes` extracted cleanly, stopping exactly at the next `##` of equal or shallower level |
| A-03 | Reusing `TicketActions` inside the drawer needs no prop it does not already take | high | no | One extra prop threaded through; a small, contained change | confirmed | Read `ticket-actions.props.ts` — `repositoryId`, `slug`, `ticketId`, `status`, `untickedCount`, `lastSubmittedBy`, `compact` are exactly what the row already passes it |
